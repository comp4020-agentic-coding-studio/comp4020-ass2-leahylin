---
title: Assessment
description:
  Four pieces of work, each testing a different module — an ethnography, an
  essay, a design project, and showing up with your fieldwork.
heroTitle: Assessment
heroImage: "/src/assets/images/assessments-hero.avif"
heroImageAlt:
  A fanned stack of marked paper, teal and gold folders showing between the
  sheets, a binder clip holding the top corner. The uppermost page carries
  handwritten notes with corrections in red ink, a grading stamp, and a
  percentage symbol, on a warm cream ground.
---

import Callout from "astro-theme-university/components/Callout.astro";
import AssessmentsGrid from "../../components/AssessmentsGrid.astro";
import AssessmentWeightChart from "../../components/AssessmentWeightChart.astro";

{/*
  Same contain/right-align override as the other index heroes: a vignette on
  its own cream, not a full-bleed scene. Background sampled at #faf3da
  straight from this asset (nudged to #fbf7de per request — still close
  enough to the sampled tone that no seam reappears), not the theme's
  --at-bg-alt token, for the same seam reason recorded on those pages.

  `.at-hero-title` is pinned to a fixed dark ink in light mode rather than
  `var(--at-text)`, since that token is light-dark()-aware and this cream
  band doesn't move — flipped explicitly to white under [data-theme="dark"]
  alongside the band itself, which goes to --at-black. The image is dimmed
  in dark mode so its baked cream doesn't sit as a bright box on black.
*/}

<style>
  {`
    .at-hero {
      background: #fbf7de;
    }
    .at-hero-image {
      object-fit: contain;
      object-position: right center;
      outline: none;
    }
    .at-hero-content {
      background: none;
    }
    .at-hero-title {
      color: oklch(from var(--at-primary) 20% 0.01 h);
      text-shadow: none;
    }
    [data-theme="dark"] .at-hero {
      background: var(--at-black);
    }
    [data-theme="dark"] .at-hero-title {
      color: var(--at-white);
    }
    [data-theme="dark"] .at-hero-image {
      filter: brightness(0.82) contrast(0.94);
    }
    @media (width < 48rem) {
      .at-hero-image {
        object-position: center top;
        opacity: 0.2;
      }
    }

    /* Same house-gold callout treatment as the Lectures and Sessions pages:
       a left rule in --at-primary instead of the theme's top rule in
       --at-info (a warm grey), and a soft primary tint instead of the
       tertiary-grey one. */
    .at-callout--info {
      --at-accent: var(--at-primary);
      --at-accent-soft: light-dark(
        rgb(from var(--at-primary) r g b / 8%),
        rgb(from var(--at-primary) r g b / 16%)
      );
      border-block-start: none;
      border-inline-start: 4px solid var(--at-accent);
    }
  `}
</style>

<AssessmentWeightChart />

Each assessment takes one module and asks you to do something with it rather
than recall it. The [Fridge Ethnography](/assessments/fridge-ethnography/)
applies Module 2's micropolitics to a real household; the
[Critical Essay](/assessments/critical-essay/) works in Module 3, where the
argument becomes measurable; the
[Counter-Fridge](/assessments/counter-fridge/) answers Module 4 by proposing
something to build instead. [Participation](/assessments/participation/) isn't tied to a single module — it's
the 10% for showing up to every tutorial with the thing that week needs,
Module 1 included.

**Three of the four cannot be written from the library.** The ethnography needs
a household that consented to being observed, the essay is strongest when it
rests on your own audit data, and the design project grows out of the week 10
and 11 tutorials. Fieldwork can be borrowed in a way an argument can't, so
submitting observations you did not make is treated as fabrication rather than
plagiarism — see [Policies](/policies/). And none of the four are marked on
agreeing with the course: the essay in particular rewards whoever presses the
strongest objection — the vaccine cold chain, reduced foodborne illness,
year-round nutrition, or the charge that blaming an appliance for consumer
capitalism is a category error — over whoever agrees with the lectures
fluently.

<Callout type="info">
  <p>
    **Fieldwork happens before the break, not during it.** The ethnography is
    due Friday 2 April, at the end of week 6 — three weeks of observation, no
    break to lean on. Settle two things by
    [week 4](/sessions/04-fridge-archaeology/), not week 6: whether the people
    you live with consent to being written about, and whether you can observe
    without tidying first.
  </p>
</Callout>

<AssessmentsGrid />
