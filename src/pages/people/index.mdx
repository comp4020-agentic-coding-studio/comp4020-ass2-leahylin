---
title: People
description: Two convenors, teaching in two registers.
heroTitle: People
heroImage: "/src/assets/images/people-hero.avif"
heroImageAlt:
  A wooden wall shelf holding a stoneware crock, a brass balance scale, glass
  apothecary bottles, a leather-bound notebook propped against a pressed-leaf
  print, a mortar and pestle, an ink bottle and a rolled scroll, with dried
  leaves scattered along the shelf, on a warm cream ground.
---

import PeopleGrid from "../../components/PeopleGrid.astro";

{/*
  Same contain/right-align override as the Lectures and Sessions heroes: a
  vignette on its own cream, not a full-bleed scene, so the theme's default
  cover-and-scrim crop would enlarge and cut it. Background sampled at
  #f5edd2 straight from this asset (nudged to #f7f3da per request — still
  close enough that no seam reappears), not the theme's --at-bg-alt token,
  for the same seam reason recorded on those pages.

  `.at-hero-title` is pinned to a fixed dark ink in light mode rather than
  `var(--at-text)`, since that token is light-dark()-aware and this cream
  band doesn't move — flipped explicitly to white under [data-theme="dark"]
  alongside the band itself, which goes to --at-black. The image is dimmed
  in dark mode so its baked cream doesn't sit as a bright box on black.
*/}

<style>
  {`
    .at-hero {
      background: #f7f3da;
    }
    .at-hero-image {
      object-fit: contain;
      object-position: right center;
      outline: none;
    }
    .at-hero-content {
      background: none;
    }
    .at-hero-title {
      color: oklch(from var(--at-primary) 20% 0.01 h);
      text-shadow: none;
    }
    [data-theme="dark"] .at-hero {
      background: var(--at-black);
    }
    [data-theme="dark"] .at-hero-title {
      color: var(--at-white);
    }
    [data-theme="dark"] .at-hero-image {
      filter: brightness(0.82) contrast(0.94);
    }
    @media (width < 48rem) {
      .at-hero-image {
        object-position: center top;
        opacity: 0.2;
      }
    }
  `}
</style>

Reading a fridge as governed space is interpretive work. Establishing that it
produces waste is quantitative. An argument built on one without the other is
either unfalsifiable or uninterpreted, so the course is convened by two people.

- **[Simone Ravel](/people/simone-ravel/)** — weeks 1–6 and 10–12. The history,
  the theory, the ethnography and its ethics.
- **[Casper Nowak](/people/casper-nowak/)** — weeks 7–9, where the argument
  becomes measurable. Supervises the Counter-Fridge project.

Either of us will redirect you rather than send you away, so ask whoever seems
closest.

<PeopleGrid />
